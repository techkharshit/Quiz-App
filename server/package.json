{
  "name": "quiz-server",
  "version": "1.0.0",
  "description": "Backend server for genetics quiz app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build required' && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "2.8.5",
    "dotenv": "^16.0.3",
    "express": "4.18.2",
    "node-fetch": "2.6.12"
  },
  "devDependencies": {
    "nodemon": "3.0.2"
  }
}
