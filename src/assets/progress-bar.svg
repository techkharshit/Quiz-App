// Inside Quiz.jsx
<div className="progress-bar">
  <svg viewBox="0 0 200 20">
    <rect width="200" height="20" fill="#e0e0e0" rx="10" ry="10"/>
    <rect
      width={`${(currentQuestionIndex + 1) * (200 / quizData.length)}`}
      height="20"
      fill="#4CAF50"
      rx="10"
      ry="10"
    />
  </svg>
</div>